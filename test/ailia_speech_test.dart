import 'package:flutter_test/flutter_test.dart';
import 'package:ailia_speech/ailia_speech.dart';
import 'package:ailia_speech/ailia_speech_model.dart';
import 'package:plugin_platform_interface/plugin_platform_interface.dart';

/*
class MockAiliaSpeechPlatform
    with MockPlatformInterfaceMixin
    implements AiliaSpeechPlatform {

  @override
  Future<String?> getPlatformVersion() => Future.value('42');
}
*/

void main() {
  /*
  final AiliaSpeechPlatform initialPlatform = AiliaSpeechPlatform.instance;

  test('$MethodChannelAiliaSpeech is the default instance', () {
    expect(initialPlatform, isInstanceOf<MethodChannelAiliaSpeech>());
  });

  test('getPlatformVersion', () async {
    AiliaSpeech ailiaSpeechPlugin = AiliaSpeech();
    MockAiliaSpeechPlatform fakePlatform = MockAiliaSpeechPlatform();
    AiliaSpeechPlatform.instance = fakePlatform;

    expect(await ailiaSpeechPlugin.getPlatformVersion(), '42');
  });
  */
}
